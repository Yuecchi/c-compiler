#ifndef TYPE_H
#define TYPE_H

// differnt possible data types
// of the language
enum type {
	INT = 1, // integers
	FUNC,    // a function which returns an integer
	ARRAY    // an array of integers
};

#endif /* TYPE_H */
